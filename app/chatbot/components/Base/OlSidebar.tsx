
import { FC } from 'react';

interface Props {
    placeholder?: string
    autofocus?: boolean
}

export const OlSidebar: FC<Props> = ({placeholder='请输入', autofocus = false}) => {
    return (
        <div>
            123
        </div>
    );
};